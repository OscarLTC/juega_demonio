---
import { Image } from "astro:assets";

import HeroImage from "../../assets/img/hero.png";
import OnlyImpage from "../../assets/img/only.png";
import OnlyImpage2 from "../../assets/img/only2.png";
import Button from "../ui/Button.astro";
---

<div
  class="flex flex-col items-center justify-center min-h-[calc(100vh-100px)] relative px-4 py-8 md:py-0"
>
  <i class="absolute -left-32 bg-intense-pink size-40 blur-3xl rounded-2xl"></i>

  <Image
    class="absolute z-0 opacity-80 w-full md:w-auto"
    src={OnlyImpage}
    alt="Background Text"
    loading="eager"
  />

  <div
    class="max-w-5xl flex flex-col md:flex-row justify-around w-full z-10 gap-8 md:gap-0"
  >
    <div
      class="flex flex-col items-center md:items-start w-full md:w-1/2 justify-center order-2 md:order-1"
    >
      <h1
        class="text-intense-pink leading-tight md:leading-28 text-5xl sm:text-7xl md:text-9xl font-family-nata max-w-xl text-center md:text-left"
      >
        UNETE A LOS
        <br />
        LOS DEMONIOS
      </h1>

      <p
        class="text-white text-base sm:text-lg md:text-xl mt-6 md:mt-10 text-center md:text-left"
      >
        La comunidad más ganadora de todo internet,
        <br class="hidden sm:block" />
        accede a <span class="text-intense-pink">premios</span> y{" "}
        <span class="text-intense-pink">descuentos</span> exclusivos.
      </p>

      <Button
        class="mt-6 md:mt-10 text-lg sm:text-xl md:text-2xl self-center"
        label="Suscríbete"
        onclick="window.location.href='/registro'"
      />
    </div>

    <div
      class="w-full max-w-[280px] sm:max-w-[320px] md:w-80 relative mx-auto md:mx-0 order-1 md:order-2"
    >
      <div
        class="absolute w-full h-9/10 bottom-0 rounded-2xl overflow-hidden bg-[#000]"
      >
        <Image
          class="absolute w-full h-full object-cover object-center z-0"
          src={OnlyImpage2}
          alt="Background pattern"
          loading="eager"
        />
      </div>

      <Image
        class="w-48 sm:w-52 md:w-60 mx-auto relative z-10"
        src={HeroImage}
        alt="Onlyrgx Valorant"
        loading="eager"
      />

      <i
        class="absolute w-full h-9/10 z-10 bottom-0 bg-linear-to-b from-transparent from-50% via-[#000]/90 via-70% to-[#000] to-90% rounded-b-2xl"
      ></i>

      <div
        class="absolute bottom-4 sm:bottom-6 md:bottom-8 left-0 right-0 text-center z-30"
      >
        <h2
          class="text-white text-4xl sm:text-6xl md:text-8xl font-family-nata font-bold"
        >
          SUSCRÍBETE
        </h2>
        <p class="text-intense-pink text-xs sm:text-sm md:text-base">
          Y participa por dos sorteos al mes
        </p>
      </div>
    </div>
  </div>
</div>
